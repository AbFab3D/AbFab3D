function main(){
	var a = 25*MM;
	var voxelSize = 0.2*MM;
	
	dest = createGrid(-a,a,-a,a,-a,a,voxelSize);
	
	sphere = new Sphere(0,0,0,a*0.9);
	
    var maker = new GridMaker();
	maker.setSource(sphere);
	maker.makeGrid(dest);
	return dest;
	
}
